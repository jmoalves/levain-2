name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version to release"
        required: true

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  build-jar:
    name: Build Standalone JAR
    uses: ./.github/workflows/reusable-build.yml
    with:
      runs-on: ubuntu-latest
      java-version: "25"
      jdk-distribution: "semeru"
      maven-goals: "clean package"
      maven-args: "-DskipTests"
      upload-artifact: true
      artifact-name: "levain-jar"
      artifact-path: "target/levain.jar"

  release:
    name: Create GitHub Release
    needs: [build-jar]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Download all artifacts
        uses: actions/download-artifact@v5
        with:
          path: release-artifacts

      - name: Prepare release files
        run: |
          bash scripts/prepare-release-files.sh \
            --jar-path "release-artifacts/levain-jar/levain.jar" \
            --release-dir "release" \
            --install-dir "install"

      - name: Generate release notes
        run: |
          bash scripts/release-notes.sh \
            --mode stable \
            --current-tag "${{ github.ref_name }}" \
            --output "release/RELEASE_NOTES.md" \
            --release-dir "release" \
            --repo "${{ github.repository }}"

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: release/*
          body_path: release/RELEASE_NOTES.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
